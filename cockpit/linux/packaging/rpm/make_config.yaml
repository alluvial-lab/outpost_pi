# Cockpit .rpm package. NOT buildable on this Mac — generated in CI (ubuntu-24.04
# with `apt install rpm` → rpmbuild; arch detected by uname -m). Validated in a
# fedora:40 container (plan premise).
#
# Resulting artifact: outpost-pi-cockpit-<version>.<arch>.rpm
display_name: Outpost-Pi Cockpit
summary: Multi-pane desktop client for Outpost-Pi
license: MIT
url: https://kevoun.com
vendor: Kevoun
packager: Kevoun
packagerEmail: <operator contact email>
group: Development/Tools
generic_name: Outpost-Pi Cockpit
# Runtime deps: GTK3 (Fedora package name).
requires:
  - gtk3
  # Plan 46 — A/V preview via media_kit (libmpv) using the system lib.
  - mpv-libs
categories:
  - Development
  - IDE
keywords:
  - pi
  - outpost-pi
  - agent
  - cockpit
startup_notify: true
icon: linux/runner/resources/app_icon_512.png
metainfo: linux/dev.kevoun.outpostpi.cockpit.metainfo.xml
description: |
  Cockpit is the Outpost-Pi desktop client: a multi-pane GUI over the Pi
  engine. It runs agents (each a `pi --mode rpc`) and terminals side by side,
  with a file tree, per-workspace worktrees, and daemon scheduling.
