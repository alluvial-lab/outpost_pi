// Smoke test replaced — original counter widget no longer exists.
// Real widget tests live in test/ui/.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — app rebuilt with go_router', () {
    expect(true, isTrue);
  });
}
